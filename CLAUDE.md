# viewlyt — instruções para agentes de código

As instruções deste projeto são agnósticas de ferramenta e vivem em
[`AGENTS.md`](AGENTS.md) (o padrão multiagente lido nativamente por Codex,
Cursor, Copilot e outros). Este arquivo só as reexporta para o Claude Code,
que carrega `CLAUDE.md` via `@import`:

@AGENTS.md
